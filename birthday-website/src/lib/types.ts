export interface Memory {
  title: string;
  description: string;
  date: string;
  image: string;
}

export interface CountdownTime {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}
